┌────────────┐
│ MCP Client │  (planner / orchestrator / future LLM)
└─────┬──────┘
      │ MCP calls
┌─────▼────────────────────────┐
│ MCP Server (mcp_runtime)     │
│                              │
│  fetch_static(state)         │
│  fetch_selenium(state)       │
│  extract_rule(state)         │
│  extract_llm(state)          │
│  summarize(state)            │
│                              │
└─────┬────────────────────────┘
      │ returns updated state
┌─────▼──────┐
│ AgentState │
└────────────┘